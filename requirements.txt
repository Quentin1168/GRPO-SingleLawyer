# ============================================================
# GRPO-SingleLawyer — direct dependencies only
# Transitive deps are resolved by uv/pip; exact versions in requirements.lock
# Python: >=3.12,<3.13 (required by ART runtime)
# ============================================================

# --- Core RL training cluster ---
# ART owns: torch, vllm, unsloth, unsloth_zoo, trl, peft, xformers,
# bitsandbytes, datasets, accelerate, triton. NEVER upgrade those individually.
openpipe-art[backend]==0.5.17   # PINNED: 0.5.18 LocalBackend crashes without megatron extra
                                 # (eager import of megatron.core in get_model_config)

# --- Overrides within ART's declared ranges (declared-compatible but actually broken) ---
transformers==4.56.2             # PINNED: 4.57 breaks unsloth 2026.3.3
                                 # (NameError: auto_docstring — unsloth#3436)

# --- Embeddings / retrieval ---
sentence-transformers<5.2        # PINNED: 5.2+ requires transformers>=5 & hf-hub>=1.3
                                 # (incompatible with our transformers-4.x era stack)

# --- App-level dependencies ---
cn2an                            # Chinese numeral → arabic conversion (law checker)
jieba                            # Chinese tokenization
pandas                           # CSV dataset loading
numpy
openai                           # OpenRouter client (opponent + judge)
python-dotenv                    # loads OPENROUTER_API_KEY / WANDB_API_KEY from .env
wandb                            # experiment tracking (ART auto-logs when WANDB_API_KEY set)